const app = require('../../app.module');
(() => {
    app.service(':upperCamelService', :upperCamel);

    :upperCamel.$inject = [];

    function :upperCamel() {
        this.name = ':upperCamelService';
    }
})();
